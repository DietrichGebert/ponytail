---
description: Create or simplify a minimal implementation plan, without implementing
---

Task: $ARGUMENTS. Read the request, project rules, and relevant code. Create or simplify the smallest implementation plan that preserves the requested outcome and safety checks. Prefer reuse, stdlib, native features, and installed dependencies. Give concrete steps, affected paths, and a runnable verification check; explain material cuts. State assumptions and ask only about consequential unknowns the project cannot answer. Use the host plan review mechanism when required. Present the plan only: do not implement, install, commit, or write a file unless requested. This is a one-task mode.
