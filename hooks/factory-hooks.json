{
  "description": "ponytail - Factory Droid lifecycle hooks. Injects the ruleset at SessionStart (filtered to the active level) and tracks /ponytail mode switches on UserPromptSubmit. PONYTAIL_HOST=factory tells ponytail-runtime to keep state under ~/.factory and emit the hookSpecificOutput JSON shape Factory reads.",
  "hooks": {
    "SessionStart": [
      {
        "matcher": "startup|resume|clear|compact",
        "hooks": [
          {
            "type": "command",
            "command": "PONYTAIL_HOST=factory node \"${DROID_PLUGIN_ROOT}/hooks/ponytail-activate.js\"; exit 0",
            "timeout": 5
          }
        ]
      }
    ],
    "UserPromptSubmit": [
      {
        "hooks": [
          {
            "type": "command",
            "command": "PONYTAIL_HOST=factory node \"${DROID_PLUGIN_ROOT}/hooks/ponytail-mode-tracker.js\"; exit 0",
            "timeout": 5
          }
        ]
      }
    ]
  }
}
